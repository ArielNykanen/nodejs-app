<!-- <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">FTC</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
    aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <% if (isAuth) { %>
      <li class="nav-item ">
        <a class="nav-link <%= path === '/work-time' ? 'active' : '' %>" href="/work-time">שעות עבודה</a>
      </li>

      <li class="nav-item ">
        <a class="nav-link <%= path === '/deliveries' ? 'active' : '' %>" href="#">משלוחים</a>
      </li>

      <li class="nav-item ">
        <a class="nav-link <%= path === '/tips' ? 'active' : '' %>" href="/tips">טיפים</a>
      </li>
     <% } %>
      <% if (!isAuth) { %>
      <li class="nav-item">
        <a class="nav-link <%= path === '/login' ? 'active' : '' %> " href="/login">התחבר</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= path === '/register' ? 'active' : '' %>" href="/register">הירשם</a>
      </li>
      <% } else { %>
      <li class="nav-item">
        <form action="/logout" method="POST">
          <button class="btn btn-danger">התנתק</button>
        </form>
      </li>
      <% } %>
     
    </ul>
    <span class="navbar-text">
      Navbar text with an inline element
    </span>
  </div>
</nav> -->
<div class="row">
  <% if (isAuth) { %>
    <div class="col-12 text-right border border-danger my-2 p-2">
      <div class="row text-right">
<div class="col-4"></div>
        <div class="col-4">
          <form action="/add-expenses">
            <button class="btn btn-danger">הוצאות</button>
          </form>
        </div>
        <div class="col-4">
          <form action="/logout" method="POST">
            <button class="btn btn-danger">התנתק</button>
          </form>
        </div>
      </div>
    </div>
  <div class="col-12 text-center border border-warning p-2">
  <a class="btn btn-warning" href="/tips">טיפים</a>
  <a class="btn btn-warning" href="/work-time">שעות עבודה</a>
  <a class="btn btn-warning" href="/statistics">סטטיסטיקה</a>
  </div>
  <% } else { %>
  <div class="col-12 text-center border border-warning p-2">
    <a class="btn btn-warning" href="/login">התחבר</a>
    <a class="btn btn-warning" href="/register">הירשם</a>
  </div>
  <% } %>

</div>